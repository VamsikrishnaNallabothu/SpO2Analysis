var mongoose     = require('mongoose');
var Schema       = mongoose.Schema;

var uservitalsSchema   = new Schema({
    userId:
});

module.exports = mongoose.model('uservitalsobj', ProductsSchema);